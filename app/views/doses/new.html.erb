<h1> Want to add a dose to <%= @cocktail.name %> ? </h1>

<%= simple_form_for([@cocktail, @ingredient, @dose]) do |f| %>
  <%= f.label :description %>
  <%= f.text_field :description %>
  <%= f.association :ingredient %>
  <%= f.submit "Charge my cocktail !", class: "btn btn-primary" %>
<% end %>
